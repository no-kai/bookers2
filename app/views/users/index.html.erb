<h1>Users</h1>

<% @users.each do |user| %>
<div>
  Image Name
  <%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"%>
  <p>name：<%= user.name %></p>
  <%= link_to "Show", "/users/#{user.id}/" %>

</div>
<% end %>


